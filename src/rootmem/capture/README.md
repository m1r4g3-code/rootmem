# capture

Reserved for Phase 1: session-lifecycle hooks, webhook capture, and batch
import — the ingestion layer that feeds the extraction pipeline. Phase 0's
`remember` MCP tool is the only write path today; there is no automatic
capture yet.
