# extraction

Reserved for Phase 1/2: LLM-based fact/event/entity/relation extraction from
captured conversation/session data, plus contradiction detection. Phase 0
makes zero LLM calls — every memory is explicit, agent-authored content via
the `remember` tool, not inferred.
